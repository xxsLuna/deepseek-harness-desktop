node_modules
dist/
build/
out/
spike/
# Every client bundle build-client.mjs generates. A glob, not a line per
# package: the per-package list did not cover a new one, and the artifact
# landed in a commit before anyone noticed.
packages/*/lib/client.js
*.log
.DS_Store
dev-overrides.css
dev-eval.js
dev-eval.out.json
dev-capture.request
dev-capture.png
dev-click.request

# Agent worktrees: full repo copies, created under .claude by isolated subagents.
.claude/worktrees/
